🧠 ProtBert Model (not included)

The pre-trained ProtBert model is required for protein encoding.
To download automatically, MolHuiTu will fetch it from HuggingFace
on first run. Alternatively, you may manually download and place it here:

  https://huggingface.co/Rostlab/prot_bert_bfd

Expected structure:
  protbert_model/
  ├── config.json
  ├── pytorch_model.bin
  ├── tokenizer.json
  └── vocab.txt
